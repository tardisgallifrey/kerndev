# An array as a parameter to a kernel module

This comes from:

https://devarea.com/linux-kernel-development-kernel-module-parameters/

It is a short module on using an array as argument to a parameter.