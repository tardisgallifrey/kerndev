# Setting Module parameters with an enum

This comes from:

https://devarea.com/linux-kernel-development-kernel-module-parameters/

It is another form of parameter validation by using an enum to set the choices.